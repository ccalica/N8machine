# playground.cfg - simplified linker config for GDB test programs
# No cc65 runtime dependency. Programs are bare-metal 6502.
#
# Vectors go to a separate .vec file so the main binary only
# contains code/data and doesn't pad to 8KB (which would overwrite
# the kernel at $F000 when loaded via GDB).

MEMORY {
    ZP:  start =   $00, size =  $100, type = rw, define = yes;
    RAM: start =  $400, size = $BC00, define = yes;
    ROM: start = $E000, size = $2000, file = %O;
    VEC: start = $FFFA, size =    $6, file = "%O.vec";
}

SEGMENTS {
    ZEROPAGE: load = ZP,  type = zp,  optional = yes;
    BSS:      load = RAM, type = bss, optional = yes, define = yes;
    DATA:     load = ROM, type = rw,  run = RAM, optional = yes, define = yes;
    CODE:     load = ROM, type = ro;
    RODATA:   load = ROM, type = ro,  optional = yes;
    VECTORS:  load = VEC, type = ro,  start = $FFFA;
}
